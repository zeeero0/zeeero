
# 🛡️ SocialBoost Morocco - دليل التشغيل السريع

هذا التطبيق مبرمج ليعمل في بيئة **Node.js**. إذا كنت أنت أو صديقك تشغلانه لأول مرة، اتبع الخطوات التالية:

## 1. المتطلبات الأساسية (مرة واحدة فقط)
*   **Node.js:** قم بتحميله وتثبيته من [nodejs.org](https://nodejs.org/) (اختر نسخة LTS).
*   **قاعدة البيانات (اختياري):** التطبيق مجهز بـ **وضع العرض (Demo Mode)**، أي أنه سيعمل حتى لو لم يكن لديك MySQL مثبت. لكن إذا أردت حفظ البيانات بشكل دائم، استخدم XAMPP وأنشئ قاعدة بيانات باسم `socialboost_db` باستخدام ملف `schema.sql`.

## 2. كيف تشغل التطبيق؟
لقد أضفنا ملفاً باسم `lancer_application.bat`.
1.  انقر مرتين على `lancer_application.bat`.
2.  سيقوم الملف بتثبيت كل المكتبات الناقصة تلقائياً.
3.  سيفتح السيرفر (Port 3001) والواجهة (Port 5173).
4.  افتح المتصفح على: `http://localhost:5173`

## 3. إعدادات الإيميل
التطبيق يستخدم إيميل `moha.taibi@edu.umi.ac.ma` لإرسال أكواد التحقق (OTP). تم ضبط الإعدادات لتعمل مباشرة.

---
**SocialBoost Morocco Security Team 🇲🇦**
